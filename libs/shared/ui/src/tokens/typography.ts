// This file is auto-generated by Style Dictionary.
// Edit libs/shared/ui/src/tokens/tokens.json (via Tokens Studio) — do not edit directly.
//
// Font loading note: font families map to named variants loaded via
// @expo-google-fonts/inter in apps/mobile/src/app/_layout.tsx.
// Android requires a dedicated file per weight — fontWeight alone is ignored
// when using @expo-google-fonts, so each style references its exact variant name.

import { TextStyle } from 'react-native';

export const typography = {
  // Brand name / hero heading
  display: {
    fontFamily: 'Inter_700Bold',
    fontWeight: '700' as const,
    fontSize: 24,
    lineHeight: 30,
    letterSpacing: -0.6,
  } satisfies TextStyle,

  // Body copy
  body: {
    fontFamily: 'Inter_400Regular',
    fontWeight: '400' as const,
    fontSize: 16,
    lineHeight: 24,
  } satisfies TextStyle,

  // Input field text and placeholder
  inputText: {
    fontFamily: 'Inter_400Regular',
    fontWeight: '400' as const,
    fontSize: 16,
    lineHeight: 19,
  } satisfies TextStyle,

  // Form labels, medium emphasis
  label: {
    fontFamily: 'Inter_500Medium',
    fontWeight: '500' as const,
    fontSize: 14,
    lineHeight: 20,
  } satisfies TextStyle,

  // Helper text, secondary copy
  bodySmall: {
    fontFamily: 'Inter_400Regular',
    fontWeight: '400' as const,
    fontSize: 14,
    lineHeight: 20,
  } satisfies TextStyle,

  // Inline links
  link: {
    fontFamily: 'Inter_600SemiBold',
    fontWeight: '600' as const,
    fontSize: 14,
    lineHeight: 20,
  } satisfies TextStyle,

  // Primary CTA button label
  button: {
    fontFamily: 'Inter_600SemiBold',
    fontWeight: '600' as const,
    fontSize: 16,
    lineHeight: 24,
  } satisfies TextStyle,

  // Social / secondary button label
  buttonSecondary: {
    fontFamily: 'Inter_500Medium',
    fontWeight: '500' as const,
    fontSize: 14,
    lineHeight: 20,
  } satisfies TextStyle,

  // Divider labels, captions
  caption: {
    fontFamily: 'Inter_400Regular',
    fontWeight: '400' as const,
    fontSize: 12,
    lineHeight: 20,
  } satisfies TextStyle,
} as const;

export type TypographyToken = keyof typeof typography;
